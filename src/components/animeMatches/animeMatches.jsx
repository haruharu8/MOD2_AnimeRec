import React from 'react'

// this handles anime that has matched the user selected 

const AnimeMatches = () => {
  return (
 
    <div>
        {/* below should somehow swap the questiions with the matched anime*/}
        AnimeMatches
    </div>
  )
}

export default AnimeMatches